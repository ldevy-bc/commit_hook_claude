{
  "main_branch": "main",
  "lint": "pnpm lint",
  "require_version_bump": ["package.json"],
  "confirm": [
    "Does this PR need a newer shared library build? If so run /bump-exchanges-version first."
  ],
  "bump_hint": "Run /bump-exchanges-version to fix the version, then retry.",
  "ask_on_pass": true
}
